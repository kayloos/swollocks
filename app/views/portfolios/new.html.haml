= form_for(@portfolio) do |f|
  = render 'shared/error_messages', :object => f.object
  .field
    = f.label :Navn
    %br/
    = f.text_field :name
  .field
    = f.label :start_amount
    %br/
    = f.text_field :start_amount, :value => 50000
  = f.hidden_field :deliver_mail, :value => false
  .field
    = f.submit "Tilføj liste"
